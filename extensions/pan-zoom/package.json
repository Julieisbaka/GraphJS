{
  "name": "graphjs-extension-pan-zoom",
  "version": "0.0.4",
  "description": "First-party pan and zoom extension for GraphJS.",
  "type": "module",
  "main": "./index.js",
  "exports": {
    ".": "./index.js"
  },
  "files": [
    "index.js",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "keywords": [
    "graphjs",
    "extension",
    "pan",
    "zoom",
    "plugin"
  ],
  "license": "MIT",
  "peerDependencies": {
    "graphjs": ">=0.0.1"
  },
  "scripts": {
    "lint": "node --check index.js",
    "build": "esbuild index.js --bundle --format=esm --outfile=dist/index.js && esbuild index.js --bundle --format=esm --minify --outfile=dist/index.min.js"
  }
}
